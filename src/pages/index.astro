---
import Layout from '../layouts/Layout.astro';
import { HomePage } from '../react/pages/HomePage';

const currentPath = Astro.params.path;
---

<Layout>
	<HomePage currentPath={currentPath ?? ''} client:only="react" />
</Layout>
